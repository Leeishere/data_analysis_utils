# data_analysis_utils

Reusable Python package for `AnalyzeDataset` and its supporting statistical helper modules.

## Install locally

From this repository:

```bash
python3 -m pip install -e .
```

Then import it from any project in the same Python environment:

```python
from data_analysis_utils import AnalyzeDataset
```

The Streamlit app lives in `apps/data_analyzer_app.py`, and the notebook quickstart lives in `examples/analyze_dataset_quickstart.ipynb`.

## GitHub remote

This repository is ready for a GitHub remote named:

```bash
git remote add origin git@github.com:Leeishere/data_analysis_utils.git
git push -u origin main
```

Creating or pushing the remote requires your GitHub authentication.
